#include "../src/utils/mutf8.hpp"
#include <iostream>
#include <locale>
#include <codecvt>

using namespace cocotools;

int main() {
    // std::vector<BYTE> vec({0x48 ,0x65 ,0x6C ,0x6C ,0x6F ,0x20 ,0xE4 ,0xB8 ,0x96 ,0xE7 ,0x95 ,0x8C ,0x21 ,0x20 ,0x53 ,0x61 ,0x6E ,0x74 ,0xC3 ,0xA9 ,0xF0 ,0x9F ,0x8D ,0xBB});
    // std::cout << std::string(vec.begin(), vec.end()) << std::endl;

    // std::string fcd = Utf16fromByteCode(187);
    // std::cout << fcd << std::endl;
    
    std::vector<BYTE> bytes = {0x48, 0x65, 0x6c, 0x6c, 0x6f, 0x20, 0xe4, 0xb8,
      0x96, 0xe7, 0x95, 0x8c, 0x21, 0x20, 0x53, 0x61,
      0x6e, 0x74, 0xc3, 0xa9, 0xed, 0xa0, 0xbc, 0xed,
      0xbd, 0xbb};
    std::string str = decodeMUTF8FromBytes(bytes);
    std::cout << str << std::endl;
    std::cout << std::string(bytes.begin(), bytes.end()) << std::endl;
    // std::wstring_convert<std::codecvt_utf8<char16_t>, char16_t> converter;
    // std::cout << converter.to_bytes(str) << std::endl;
    return 0;
}